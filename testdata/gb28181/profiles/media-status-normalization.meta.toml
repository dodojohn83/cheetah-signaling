source = "synthetic"
standard = "GB/T 28181-2016"
profile = "gb4-comp-004-media-status-normalization"
expected = "A MediaStatus NOTIFY is accepted only when the MediaStatus capability is active (otherwise 400 Bad Request). The canonical NotifyType 121 and any configured vendor stopped code (here 99) normalise to MediaStatusOutcome::Stopped; all other values normalise to Unknown."
desensitization = "Device IDs replaced with synthetic dummy values"
license = "CC0-1.0"
manufacturer = "Uniview"
model = "NVR-SYNTHETIC"
firmware = "V1.0"
risk = "Treating a non-standard NotifyType as 'stopped' could prematurely end a media session if configured incorrectly. The mapping is capability-gated (MediaStatus), requires an explicit per-profile stopped-code list, and only reads the typed NotifyType identifier: no RTP/RTCP/PS/TS/ES payload is inspected."
regression_test = "cheetah-gb28181-module register_tests::{media_status_rejected_without_capability, media_status_canonical_normalises_to_stopped_with_capability, media_status_vendor_code_normalises_to_stopped}"
removal_criteria = "Remove the vendor stopped-code entry once the targeted firmware emits the canonical NotifyType 121, or when the device model is retired from the fleet."
