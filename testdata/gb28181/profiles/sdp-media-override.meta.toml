source = "synthetic"
standard = "GB/T 28181-2016"
profile = "gb4-comp-003-sdp-media-override"
expected = "A device SDP answer using non-baseline payload type 34 and the vendor 'vendorext' attribute is accepted only when SdpMediaOverride is enabled and both the payload type and attribute name are explicitly listed; otherwise the offer is rejected and the dialog is torn down (ACK+BYE)."
desensitization = "Device IDs, addresses and SSRC replaced with synthetic dummy values"
license = "CC0-1.0"
manufacturer = "Hikvision"
model = "DS-2CD-SYNTHETIC"
firmware = "V5.5.0"
risk = "Relaxing the SDP payload/attribute allow-list can let signaling admit media descriptions the media plane cannot handle, or mask malformed answers. Scope is bounded: only the explicitly listed decimal payload types and attribute names are accepted, everything else stays strict, and no RTP/RTCP/PS/TS/ES payload bytes are ever parsed or stored."
regression_test = "cheetah-gb28181-module media::tests::override_tests::{non_baseline_payload_rejected_by_strict_default, non_baseline_payload_accepted_with_profile_override, unknown_attribute_rejected_by_strict_default, unknown_attribute_accepted_with_profile_override}"
removal_criteria = "Remove when the targeted firmware is retired from the fleet, or once the device is confirmed to emit only GB28181 baseline payload types (0, 8, 96-127) and baseline SDP attributes without a profile override."
